# Loaded for login shells only (before .zshrc).
# Use for: login-time setup, environment vars that should only run once per login.
# Avoid: aliases, prompt, anything needed in non-login interactive shells.
